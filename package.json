{
  "name": "moeum-eslint-prettier-toolkit",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "publish:cli": "turbo run deploy --filter=@moeum/eslint-prettier-cli",
    "publish:base": "turbo run deploy --filter=@moeum/eslint-base",
    "publish:react": "turbo run deploy --filter=@moeum/eslint-react"
  },
  "devDependencies": {
    "prettier": "^3.2.5",
    "turbo": "^2.3.3",
    "typescript": "5.5.4"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=18"
  }
}
